# No Specification Changes

Fixes bugs in `scripts/test-container.sh`:
- Guard unconditional `shift`.
- Add `cargo-nextest` install in `all` mode.

No new/modified capabilities. Impl details (design.md).